# Synthetic suspicious fixture

`setup.js` **throws immediately**. It is labeled for static tests (spawn/net/credential path *strings* only).

This is not a Shai-Hulud sample and must never be executed on a host or runner.
