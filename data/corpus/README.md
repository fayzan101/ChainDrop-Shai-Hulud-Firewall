# RAG document list

Place `urls.txt` and `documents.jsonl` here. Ingest fetches vendor pages at corpus-build time.

Any row tagged `chaindrop` must set `exclude_from_heldout_corpus: true`. See [docs/dataset.md](../../docs/dataset.md) and [docs/references.md](../../docs/references.md).
